import Vue from 'vue';
import Router from 'vue-router';

Vue.use(Router);

// Router options
const router = new Router({
  hashbang: false,
  root: '/',
  linkActiveClass: 'active',
});

// Routes
export default router.map({

});
